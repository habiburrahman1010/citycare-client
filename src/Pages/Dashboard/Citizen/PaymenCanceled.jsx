import React from 'react';

const PaymentCanceled = () => {
    return (
        <div>
            <h2 className="text-4xl">payment canceled .please try again</h2>

            <button className='btn btn-primary text-secondary'>try again</button>
        </div>
    );
};

export default PaymentCanceled;